{
  "name": "@platforma-open/milaboratories.mixcr-library-builder.ui",
  "version": "2.1.0",
  "type": "module",
  "scripts": {
    "dev": "ts-builder build --target block-ui --dev-server",
    "watch": "ts-builder build --target block-ui --watch",
    "build": "ts-builder build --target block-ui",
    "preview": "ts-builder build --target block-ui --dev-server --preview",
    "lint": "eslint .",
    "type-check": "ts-builder types --target block-ui"
  },
  "dependencies": {
    "@platforma-open/milaboratories.mixcr-library-builder.model": "workspace:*",
    "@platforma-sdk/ui-vue": "catalog:",
    "@platforma-sdk/model": "catalog:",
    "vue": "catalog:"
  },
  "devDependencies": {
    "@milaboratories/ts-builder": "catalog:",
    "@milaboratories/ts-configs": "catalog:",
    "@platforma-sdk/eslint-config": "catalog:",
    "eslint": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
